/* TODO:
    so a lot of this should probably be wrapped in a try/catch because
    when using the contract abi, there's a very good chance for some contracts
    that the tokenURI won't be able to render the image, so handle errs
    accordingly and init vals with null
    

*/



const flipped_nfts = require('../data/flipped_nfts.json')

const addImage = () => {

}